module.exports = {
    name: 'alertMaintenance',
    category: 'developer',
    description: 'Warns staff about bot maintenance',
    args: true,
    guildOnly: true,
    async execute(client, message, args) {
        if(msg.content
}
}
